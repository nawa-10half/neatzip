# macOS
.DS_Store

# Xcode / SwiftPM
DerivedData/
build/
*.xcuserstate
xcuserdata/
.swiftpm/
.build/

# XcodeGen 生成物（project.yml から `xcodegen generate` で再生成）
*.xcodeproj
App/Info.plist
Extension/Info.plist
Extension/NeatZipFinderExtension.entitlements

# 設計/意思決定ログは非公開（公開リポには含めない・履歴からも除去済み）。
# 原本はローカル作業コピー＋ private バックアップ（~/Developer/neatzip-private/）。
docs/DESIGN.md
docs/bench-v0.1.1.md

# 動作確認の生成物（クリーンZIP の出力やテスト用フォルダ）
*.zip
/Test/

# Serena MCP の作業用ディレクトリ（プロジェクト固有のキャッシュ）
.serena/

# 速度プロト（DESIGN §5 #11 / prototype/README.md）: 自作ハーネスとパッチは追跡、
# vendored 上流ソース・ビルド成果物・計測データ・ビルド済みバイナリは除外
prototype/vendor/
prototype/corpus/
prototype/t/
prototype/proto
prototype/bench
prototype/probe_crypto
prototype/probe_pbkdf2
prototype/probe_pbkdf2_par
